let login = {
    userName: 'asookaye',
    passWord: 'value',    
  };

  debugger;
  let database = [login];

  let newsfeed = [
    {userName: 'asookaye', timeline: 'xxx'},
    {userName: 'as', timeline: 'xxxx'},
    {userName: 'aye', timeline: 'xxxcc'},
] ;

const letterType = sample.indexOf('j');
let fred = prompt('what is your age');

//if (fred => && fredNot < fredYes) {};

if (fred > 2) {
  alert('your are too young');
} else if (fred == 2) {
  alert('your are dumb')
} else {
  alert('go home');
};
///////
switch (users.length){
  case 0:
      console.log('no one is online');
  break;

  case 1:
      console.log(`${users[0]} 'is online'`);
  break;

  case 2:
  break;

  default:
  break;
}